# AI 辅助全栈开发培训

面向熟悉 Vue / React、JavaScript / TypeScript，希望开始承担后端开发的同事。

本文分为两部分：第一部分介绍以 Codex 为例的 AI 编程工程方法；第二部分介绍 Python 与 TypeScript + NestJS 两套后端技术栈，以及前后端同仓库的组织方式。

培训目标：能够把需求整理成可执行的方案，在 AI 协助下完成页面、接口和数据库的联调，并解释、验证和维护最终代码。

> 文档核对日期：2026-09-20。模型名称、客户端入口和 API 配置会变化，涉及这些内容时以安装版本、账号可用选项及官方文档为准。文中的目录、业务规则和提示词是本培训的建议示例。

**阅读导航**

- [第一部分：AI 编程工具与工程技巧](#part-ai)：基础环境、思想转变、需求与方案落盘、项目规则、模型与思考等级、Token 管理、第三方 API、排错与复用。
- [第二部分：常用后端技术栈与项目组织](#part-backend)：Python、TypeScript + NestJS、frontend/backend 同仓库结构、接口契约、联调与交付。

<a id="part-ai"></a>

## 第一部分：AI 编程工具与工程技巧

### 1.1 基础环境

**要使用对AI友好的工具**。
文档使用markdown，表格使用csv，尽量不使用word、Excel、pdf，这些类似于二进文件，AI操作起来要借助工具，比较费token。markdown、csv是文本文件，类似于txt，AI可以直接读取，并且可以方便的进行对比，明确修改了那一行。

![](img\查看修改行.png)

不得不使用word文档时，建议先让AI将word转为markdown，对markdown进行后续的编辑，最终确定之后，再转为word发出去。

代码管理使用git，而不要使用svn。

Git 支持本地提交和轻量分支，方便在 AI 修改代码前保存版本，修改后检查差异、按需回退。使用 worktree 还能让多个任务在独立目录中进行，减少相互干扰。这些能力适合 AI 辅助开发中频繁试验、小步验证的工作方式。
还可以与GitHub CLI搭配使用，安装GitHub CLI之后，可以让AI直接操作GitHub，例如跟AI说：`创建GitHub私有仓库，并上传代码`。就可以直接换台电脑继续干活，减少手动创建仓库上传代码这些繁琐操作。

### 1.2 思想转变

使用 AI 后，我们要重点关注这3个问题：
- **要做什么？** 需求、业务规则和范围是否明确。比如：需不需要维护数据
- **为什么这样做？** 技术方案是否适合现有项目。比如：客户要自己部署，部署后端太麻烦，所以搞一个json文件存储数据
- **怎么证明做对了？** 是否有可执行的验收步骤和实际结果，方便AI自测。比如：文件夹、json、前端看到的数据完全一致。

至于如何做，可以由AI主导，程序员关注整体，不必太关注实现细节。

### 1.3 开发流程
尤其是复杂需求与大功能，如果只写一段提示词就直接让 AI 开始写代码，输出的结果很多时候都不是想要的，还要浪费token去改。并且如果时间跨度比较长，自己会忘，AI也没有足够的上下文去记忆。所以一定要先形成文档。所以按照以下步骤推进：

> **讨论需求 → 确认方案 → 写入 Markdown → 分阶段实现 → 验证**

最终让 AI 输出一个结构清晰的 Markdown 文档（如 `docs/plan.md`）。这份文档是后续指导 AI 逐步编码的核心依据，应重点包含以下三部分：

1. **确认的需求**
   - **业务范围**：明确“本次要做什么”以及明确“**本次不做什么**”（防止AI加戏）。
   - **核心规则**：字段限制、权限边界、异常情况处理（未登录、非法入参、数据不存在等）。
2. **每个功能的详细设计**
   - **界面设计**：页面布局、关键表单与数据展示、交互反馈。
   - **技术设计**：技术选型、接口约定（URL、请求/响应结构、状态码）、数据表或存储结构。
   - **实现思路**：数据流转逻辑、前后端分工、核心业务处理流程。
   - **验收标准**：明确“怎样证明做对了”，给出可执行的验证步骤与期望结果，便于 AI 自测和人工验收。
3. **拆分好的实现阶段**
   - **渐进交付**：将大任务拆分为若干个可独立运行、前后端打通的子阶段（一次实现一个完整小闭环，严禁一次性生成大量未验证代码）。
   - **注明实现难度**：为每个阶段标注难度（如：**简单 / 中等 / 复杂**），方便后续针对不同阶段灵活选择合适的 AI 模型。

需求部分以人为主导，ai作为辅助。详细设计ai为主导，但是人一定要仔细审。

需求讨论阶参考提示词
```text
我要开发xxx功能，这个功能要求是xxxx，先不写代码，先跟我讨论，将我的需求细化完善，任何你认为不明确、不完善、不合理的地方，都要跟我沟通，直到明确需求，达成共识。
```

生成文档提示词
```text
需求已经讨论清楚，先不要写业务代码。
请把刚才确认的内容整理成技术方案文档，保存到 docs/plan.md 中，这个文档要作为后续编码的唯一依据，所以务必准确、完整。
文档必须包含以下三部分：
1. 确认的需求（明确要做和不做的范围、业务规则与异常处理）
2. 每个功能的详细设计（界面布局与交互、技术选型与接口约定、实现思路、具体的验收标准）
3. 拆分好的实现阶段（按可独立验证的小闭环拆分，并标注每个阶段的实现难度：简单 / 中等 / 复杂，以及建议使用的模型）
```

开发阶段提示词
```text
按照 docs/plan.md。开始开发【阶段一：xxx】，完成之后提交代码（一定要提交代码，万一有问题还能回退）
```

对于大型项目，建议采用逐步分解的方式。

### 1.5 模型选择与思考等级

#### 1.5.1 分开考虑模型、思考等级和工具

- **模型**影响理解、推理、编程及工具使用能力。
- **思考等级**影响模型为当前任务投入多少推理，通常也影响耗时和用量。
- **工具与权限**决定能否读取仓库、运行测试、打开浏览器或连接外部系统。

模型再强，也需要正确的项目目录、依赖环境和验证方式。遇到失败时，先分清是需求不明确、环境不可用，还是推理难度较高。

#### 1.5.2 根据工作难度选择模型

截至本文核对日期，官方模型页列有 Astra、Sol、Terra、Luna。下面将其定位映射到培训任务，具体可用性取决于账号和客户端。

| 模型示例 | 本培训中的使用建议 |
| --- | --- |
| GPT-5.6 Luna | 规则明确的文档整理、简单转换、局部小改动 |
| GPT-5.6 Terra | 日常业务功能、常规联调、范围清楚的修复 |
| GPT-5.6 Sol | 较复杂的跨文件修改、方案取舍、难定位的问题 |
| GPT-6 Astra | 长流程、跨工具、需要持续判断的复杂任务 |

可从账号默认模型开始，用团队自己的任务比较完成质量、返工次数和总用量，不必每个任务都切换模型。以上定位及可用性依据 [官方模型说明](https://learn.chatgpt.com/docs/models)。

#### 1.5.3 思考等级如何选

| 常见等级 | 建议场景 |
| --- | --- |
| Low / Light | 修改文案、局部样式、按明确规则做小改动 |
| Medium | 常规功能实现、接口联调、一般错误排查 |
| High | 跨模块重构、权限分析、数据库设计与复杂缺陷 |
| Extra High / `xhigh` | 多轮常规排查仍无法解决、需要权衡较多约束的问题 |

部分客户端还有 Max / Ultra 等选项。当前官方将 Ultra 描述为包含子智能体协作的模式，不能简单理解为单个模型多思考一点。以界面实际可选项为准，模型与供应商不一定支持相同等级。参见 [思考等级说明](https://learn.chatgpt.com/docs/models#pick-a-reasoning-effort)。

本培训建议：日常先用默认或 Medium；任务很明确时尝试降低；遇到复杂问题时，在补齐证据后再提高。思考等级不决定是否执行测试，也不保证结论正确。

在支持的 Codex CLI 中，使用 `/model` 调整模型与思考等级。也可以在启动时指定模型：

```powershell
codex --model gpt-5.6-terra
```

模型必须对当前账号或供应商可用。桌面版与 IDE 扩展使用模型选择控件，具体布局可能随版本变化。

### 1.6 节省 Token：减少无效上下文和返工

#### 1.6.1 先区分三个概念

| 概念 | 含义 | 容易产生的误解 |
| --- | --- | --- |
| 上下文窗口 | 当前一次处理能容纳的信息量 | 文件在仓库里，就一定全部在当前上下文里 |
| Token 用量 | 处理输入、输出、推理等产生的用量，口径依产品而定 | 只计算最终看到的回答文字 |
| 额度或账单 | 订阅限制、积分或 API 供应商的计费结果 | 压缩上下文后，已经使用的额度也会恢复 |

API 场景还可能有缓存输入计费。缓存受模型和供应商规则影响，不能把“内容重复”当作免费，也不能保证隔天仍然命中缓存。参见 [OpenAI Prompt Caching](https://developers.openai.com/api/docs/guides/prompt-caching)。

#### 1.6.2 优先做这些优化

1. **给出准确入口。** 告诉 AI 相关目录、功能、报错和文档位置，减少全仓库盲查。
2. **先搜索，再按需读取。** 日志先提取关键错误和必要上下文，不反复输出整个日志文件。
3. **及时保存决定。** 已确认的业务规则写入文档，减少同一问题反复讨论。
4. **控制任务范围。** 一轮完成一个目标明确的功能，避免同时引入许多无关改动。
5. **输出适度。** 要求汇报改动、验证结果和剩余问题，不必每轮重复完整代码。
6. **按任务选择模型与等级。** 简单任务用合适配置，复杂任务关注总体成功率与返工成本。
7. **按需启用工具和技能。** 只接入当前工作需要的能力，避免无用的说明和输出占用上下文。
8. **让正确的检查尽早失败。** 一次针对性的测试通常比多轮猜测更节省时间和用量。

这些是工程建议，不承诺固定节省比例。判断是否划算，应看完成整个任务的总成本。

#### 1.6.3 当天结束时：先写交接，再按需压缩

推荐顺序：

```text
检查当前改动 → 保存进度和决定 → 记录验证状态 → 按需压缩会话
```

每日收工提示词：

```text
今天先到这里。请更新 docs/handoff.md：
1. 当前目标、分支和相关提交（如果有）。
2. 今天完成了什么，哪些代码尚未提交。
3. 已确认的重要决定及对应文档。
4. 实际执行的检查、结果和未验证部分。
5. 当前问题、复现步骤、已尝试但无效的方法。
6. 明天最先执行的一到三步，以及相关文件路径。

保持简洁，核对当前文件状态，不复制整段聊天或大量日志。
```

在支持的 Codex CLI 中，`/compact` 用于压缩当前对话上下文。它适合长会话的阶段性整理；压缩摘要仍可能丢失细节，所以重要决定应先写入文件。命令行为见 [CLI 命令说明](https://learn.chatgpt.com/docs/developer-commands?surface=cli)。

**“下班前一定要压缩”不是固定省钱公式。** 压缩本身可能产生用量，不能退回已经消耗的 Token，也不能重置订阅额度。如果会话很短，或暂时不会继续，通常不必仅因为到了下班时间就压缩。

第二天继续同一目标时，可以先恢复原会话；新目标可另开会话。无论哪种方式，都可要求：

```text
先读取 AGENTS.md、docs/handoff.md 和当前任务相关方案，
再检查实际代码与 Git 状态，确认交接内容是否仍然成立。
从交接文档中的下一步继续。
```

### 1.7 接入第三方 LLM API

#### 1.7.1 先确认接入的是哪一层

| 需求 | 配置位置 |
| --- | --- |
| 让 Codex 使用另一个模型服务写代码 | Codex 的模型供应商配置 |
| 让自己的产品调用 LLM，例如总结工单 | `backend` 的业务代码和服务端环境变量 |

这两件事互相独立。更改 Codex 供应商不会自动为产品接入 AI；产品的 API Key 也不应放进前端。

#### 1.7.2 供应商兼容性需要确认什么

接入前向服务提供方确认：API 地址、模型 ID、认证方式、协议、流式响应、工具调用、用量限制，以及是否兼容所用客户端的上下文压缩机制。

**“兼容 OpenAI”不等于“完整支持 Codex”。** 有些服务只兼容普通聊天请求。本文按当前配置参考使用 Responses 协议；该参考将 `wire_api` 的支持值列为 `responses`。不要直接套用旧教程中的 `wire_api = "chat"`。参见 [供应商配置参考](https://learn.chatgpt.com/docs/config-file/config-reference)。

如果服务只支持 Chat Completions，需要选择明确支持该服务的编程客户端，或使用经过验证的协议适配网关。仅更换 URL 无法保证工具调用、流式事件和压缩都能工作。

#### 1.7.3 Codex 配置示例

以下只是占位示例，需要替换为供应商实际地址与模型 ID；本培训不绑定某一家第三方服务，也未对具体供应商做连通性验证。

用户配置通常位于 `~/.codex/config.toml`；Windows 默认对应 `%USERPROFILE%\.codex\config.toml`。如果自定义了 `CODEX_HOME`，使用该位置。先备份现有文件，再合并配置，避免重复定义同名键。

```toml
# 顶层设置放在表定义之前。
model = "YOUR_PROVIDER_MODEL_ID"
model_provider = "team_llm"

# 仅在供应商与模型支持时启用。
# model_reasoning_effort = "medium"

[model_providers.team_llm]
name = "Team LLM Gateway"
base_url = "https://llm.example.com/v1"
env_key = "TEAM_LLM_API_KEY"
wire_api = "responses"
```

`base_url` 的路径以供应商文档为准，不要未经确认就追加 `/responses`。`env_key` 填的是环境变量名称，真实密钥由该变量提供。配置字段含义见 [自定义供应商说明](https://learn.chatgpt.com/docs/config-file/config-advanced#custom-model-providers)。

可在 **PowerShell 7.1 及以上版本**中通过隐藏输入设置当前终端进程的环境变量，再从同一终端启动 Codex CLI：

```powershell
$env:TEAM_LLM_API_KEY = Read-Host "请输入供应商 API Key" -MaskInput
codex
```

`-MaskInput` 从 PowerShell 7.1 开始提供，见 [Read-Host 官方说明](https://learn.microsoft.com/en-us/powershell/module/microsoft.powershell.utility/read-host?view=powershell-7.5)。它不适用于旧版 Windows PowerShell 5.1。使用旧终端时，通过操作系统环境变量设置或团队凭据工具配置。不要把真实密钥直接写进可复制的命令、聊天、截图或仓库。

当前终端设置的变量不会自动传递给已经运行的桌面应用。桌面版需要从能获得该环境变量的环境启动；托管设备还可能受管理员策略限制。接入第三方服务意味着相关请求内容会发送给该服务，应使用公司允许的供应商。

#### 1.7.4 分层验证，避免“能回复就算成功”

在一个不含敏感数据的演示项目中依次验证：

1. **文本请求：** 模型可以正常回复。
2. **读取工具：** 能读取指定演示文件并正确描述。
3. **修改与执行：** 能修改一个简单函数，运行对应测试，再根据结果继续处理。
4. **较长流程：** 工具调用、流式响应和恢复行为稳定；需要压缩时验证是否兼容。
5. **计费与限制：** 在供应商后台核对实际用量、并发与限流情况。

| 现象 | 优先排查 |
| --- | --- |
| 401 / 403 | 密钥、模型权限、组织限制、变量是否被进程读取 |
| 404 | API 基础路径、模型 ID、协议端点是否匹配 |
| 普通聊天成功，但不能读文件或运行工具 | 工具调用格式、模型能力、客户端权限 |
| 请求中途断开 | 流式事件格式、网络代理、超时与限流 |
| 提示不支持 reasoning 参数 | 移除该参数，核实供应商支持的取值 |
| 长会话压缩失败 | 供应商是否支持客户端使用的压缩方式 |

这些状态码也可能由网关自定义，最终要结合脱敏后的错误体和供应商文档判断。第三方 API 通常有独立计费，不能把 ChatGPT 订阅当作第三方 API 额度。

### 1.8 排错、审查与可复用能力

#### 1.8.1 给 AI 完整的错误证据

```text
问题：创建工单后页面显示失败。
复现：登录普通用户，填写合法标题，点击提交。
预期：创建成功，并进入详情页。
实际：POST /api/tickets 返回 500。
相关信息：附脱敏响应、对应时段的后端日志和最近改动。

请先定位失败发生在哪一层，提出最小验证步骤，再修复根因。
不要只修改前端提示来掩盖后端错误。修复后复测原场景。
```

如果连续修复两三轮仍然重复失败，暂停继续堆补丁，整理已经证明和排除的原因，再重新分析。

#### 1.8.2 审查时看业务行为

重点检查：是否遗漏权限条件、是否信任前端传来的用户身份、是否破坏已有数据、错误是否被吞掉、测试是否真正覆盖失败场景。对重要改动，可先让 AI 按明确关注点审查，再由开发者确认。

要求交付时说明“改了什么、为什么、实际运行了什么、还没验证什么”。AI 的“完成了”不是验证证据。

#### 1.8.3 Skills 与 MCP 放在基础流程之后

| 能力 | 适合解决的问题 | 培训示例 |
| --- | --- | --- |
| Skill | 复用已经稳定的工作步骤 | 按团队规范创建一个业务模块 |
| MCP / 连接器 | 读取或操作外部工具与系统 | 获取任务信息、接口文档、设计资料 |

Skill 可以把指令与配套资源组织成可复用流程，见 [技能官方文档](https://learn.chatgpt.com/docs/build-skills)。本培训建议先把一个流程实际做顺，再提炼成技能；外部工具也按任务需要逐个接入。

多个任务需要同时修改代码时，应分清文件所有权，必要时使用 Git worktree 隔离。并行运行可以减少等待，但也可能增加总用量与合并成本，适合作为进阶内容。

<a id="part-backend"></a>

## 第二部分：常用后端技术栈与项目组织

### 2.1 选型原则：先满足业务，再增加组件

如果公司已有成熟的后端服务、认证体系或部署规范，优先沿用。对没有历史约束的新项目，可从以下两条路线中选择一条：

| 比较项 | Python + FastAPI | TypeScript + NestJS |
| --- | --- | --- |
| 更适合的起点 | 团队已有 Python 经验，或依赖数据处理、模型相关库 | 前端团队熟悉 TypeScript，主要开发业务接口 |
| 主要优势 | 容易整合 Python 生态，接口定义直观 | 前后端语言一致，模块与依赖组织明确 |
| 需要补的知识 | 类型注解、虚拟环境、同步与异步边界 | 依赖注入、模块、装饰器、运行时校验 |
| 接口协作 | OpenAPI + 生成前端客户端 | OpenAPI + 生成前端客户端，或明确管理共享契约 |
| 共同要掌握 | 数据库、事务、权限、日志、迁移、测试、部署 | 数据库、事务、权限、日志、迁移、测试、部署 |

AI 辅助编程本身不要求后端选 Python。产品需要调用 LLM API，也不意味着一定要用 Python；应看是否依赖特定生态和现有团队能力。

同一个项目初期选择一套主后端即可。数据库、API 和前端足以支撑许多起步需求；缓存、队列和多个服务在出现实际需求后再引入。

### 2.2 先理解一次请求如何流转

```text
用户填写页面
    ↓
frontend：处理交互，发送 HTTP 请求
    ↓
backend：识别身份、校验输入、执行权限和业务规则
    ↓
数据访问：读取或写入 PostgreSQL
    ↓
backend：整理公开响应，记录必要日志
    ↓
frontend：显示成功、失败、加载中或空状态
```

后端不能假定请求一定来自自己的页面。接口可能被直接调用，因此输入校验、权限检查和数据约束需要在后端落实。

| 概念 | 在工单案例中的含义 |
| --- | --- |
| 认证 | 识别当前请求是哪位用户发出的 |
| 授权 | 判断当前用户能否查看或修改这张工单 |
| 参数校验 | 标题是否为空、长度是否超限、状态是否合法 |
| 业务规则 | 已关闭工单能否编辑，谁可以重新打开 |
| 数据约束 | 创建人是否存在，必要字段是否允许为空 |
| 事务 | 创建工单与写入必要操作记录需要一起成功或一起失败 |
| 数据库迁移 | 新增优先级字段时，如何升级已有数据库 |

### 2.3 Python 技术栈

本节保留原始技术栈清单，并补充每项的职责和使用边界。Python 版本沿用 **3.12 / 3.13**：同一个项目选择其中一个明确版本，开发、测试和部署保持一致，不将它们描述为必须追随的最新版本。

#### 2.3.1 技术栈一览

| 层次 | 推荐技术 | 负责什么 |
| --- | --- | --- |
| 语言 | Python 3.12 / 3.13 | 后端业务逻辑 |
| Web 框架 | FastAPI | 路由、依赖处理、HTTP 接口 |
| API Schema | Pydantic | 定义和校验请求、响应的数据结构 |
| ORM | SQLAlchemy 2.x | 数据模型、查询和数据库会话 |
| 数据库迁移 | Alembic | 管理数据库结构的版本变化 |
| 数据库 | PostgreSQL | 持久化业务数据 |
| 缓存 | Redis | 按需缓存和其他临时数据；没有需求先不装 |
| 后台任务 | Celery | 按需异步任务、重试与独立 worker；没有需求先不装 |
| 测试 | pytest | 单元测试和接口测试组织 |
| Lint / Format | Ruff | 代码检查和格式化 |
| 依赖管理 | uv | 项目环境、依赖与锁文件 |
| 配置 | pydantic-settings | 从环境等来源加载应用配置 |
| 部署 | Docker + Docker Compose | 打包应用和组织服务 |
| Web Server | Uvicorn | 作为 ASGI 服务器运行 FastAPI 应用 |

FastAPI 提供基于类型声明的接口能力和 OpenAPI 支持，见 [FastAPI 官方说明](https://fastapi.tiangolo.com/features/)。类型注解、请求校验和业务校验各有职责，仍需开发者明确规则。

#### 2.3.2 几个容易混淆的关系

**Pydantic 与 SQLAlchemy：** Pydantic 负责接口层的数据形状与校验；SQLAlchemy 负责数据库映射与访问。创建工单的请求通常只包含标题和描述，数据库记录还会有 ID、创建人、状态和时间，两者不必相同。

**SQLAlchemy 与 Alembic：** 修改 ORM 模型不会自动、可靠地升级已经存在的生产数据库。结构变化应形成迁移文件。Alembic 自动生成的是待审查的迁移候选，涉及重命名、数据回填、删除列等操作时必须检查其含义。见 [Alembic 迁移生成说明](https://alembic.sqlalchemy.org/en/latest/autogenerate.html)。

**FastAPI 与 Uvicorn：** FastAPI 定义应用，Uvicorn 启动并服务该应用。开发时的 `--reload` 用于自动重载，不作为生产启动方式。

**异步函数与后台任务：** `async def` 不会自动把耗时任务变成可恢复的后台工作，也不会使阻塞库自动变成非阻塞。需要持久任务状态、重试或独立 worker 时，再评估 Celery。FastAPI 自带的轻量后台任务也不能直接替代可靠队列，见 [FastAPI 后台任务说明](https://fastapi.tiangolo.com/tutorial/background-tasks/)。

SQLAlchemy 的 Session / AsyncSession 应按工作单元管理，不要在并发请求或任务之间共享同一个可变会话对象。见 [SQLAlchemy Session 说明](https://docs.sqlalchemy.org/en/20/orm/session_basics.html)。

#### 2.3.3 Python 后端目录示例

下面展示仓库中 `backend/` 的内部结构；与前端共用仓库根目录，不再单独初始化嵌套 Git 仓库。

```text
backend/
├─ app/
│  ├─ __init__.py
│  ├─ main.py                    # 创建应用、注册路由
│  ├─ core/
│  │  ├─ config.py               # pydantic-settings 配置
│  │  ├─ security.py             # 身份与权限相关基础逻辑
│  │  └─ logging.py              # 日志配置
│  ├─ db/
│  │  ├─ base.py                 # ORM 模型基类
│  │  └─ session.py              # 数据库连接与会话
│  └─ modules/
│     ├─ users/
│     │  ├─ router.py
│     │  ├─ schemas.py
│     │  ├─ models.py
│     │  └─ service.py
│     └─ tickets/
│        ├─ router.py            # HTTP 路由
│        ├─ schemas.py           # 请求与响应模型
│        ├─ models.py            # 数据库模型
│        └─ service.py           # 业务规则与用例
├─ migrations/
│  ├─ env.py                     # Alembic 环境及模型元数据
│  └─ versions/                  # 数据库迁移文件
├─ tests/
│  ├─ conftest.py                # 测试环境与公共 fixture
│  ├─ unit/
│  └─ integration/
├─ alembic.ini
├─ pyproject.toml
├─ uv.lock
├─ .python-version
├─ .env.example
├─ Dockerfile
└─ README.md
```

此处省略了部分包初始化文件。复杂查询可以抽成 `repository.py`；简单模块不必为每个数据库操作再包一层。数据库连接和事务边界应统一，避免散落在路由处理函数中。

#### 2.3.4 uv 与常用开发命令

接手已经有 `pyproject.toml` 和 `uv.lock` 的项目，在 `backend` 目录执行：

```powershell
uv sync --locked
uv run uvicorn app.main:app --reload --port 8000
```

第二条命令假定 `app/main.py` 中存在名为 `app` 的 FastAPI 实例。在另一个终端运行已经配置好的检查：

```powershell
uv run pytest
uv run ruff check .
uv run ruff format --check .
```

这些是对已有项目的运行示例，不会自动生成业务应用。初始化项目时，由团队确定依赖版本、数据库驱动和目录约定，再生成锁文件。`uv.lock` 应提交，`.venv` 不提交。uv 项目工作方式见 [官方项目指南](https://docs.astral.sh/uv/guides/projects/)。

### 2.4 TypeScript + NestJS 技术栈

#### 2.4.1 为什么适合前端同事起步

前端同事可以继续使用 TypeScript、Node.js、包管理器和熟悉的调试工具，把学习重点放在业务建模、数据库与后端运行方式上。

NestJS 提供模块、Controller、Provider 和依赖注入等组织方式，适合把业务功能逐步拆清楚。基本项目结构见 [NestJS 入门文档](https://docs.nestjs.com/first-steps)。

需要明确：TypeScript 的静态类型不会自动检查实际收到的 HTTP 数据。后端仍需要运行时参数校验，也需要学习 Node.js 的事件循环与阻塞边界。

#### 2.4.2 技术栈一览

| 层次 | 推荐技术 | 负责什么 |
| --- | --- | --- |
| 运行时 | 与依赖兼容的 Node.js LTS | 执行后端应用；项目固定具体版本 |
| 语言 | TypeScript | 类型声明与业务实现 |
| Web 框架 | NestJS | 模块、路由、依赖注入与应用组织 |
| 参数校验 | DTO + class-validator + class-transformer | 配合 ValidationPipe 校验实际请求 |
| 接口文档 | `@nestjs/swagger` / OpenAPI | 描述接口并用于生成客户端 |
| ORM | Prisma | 数据模型、数据库客户端和查询 |
| 数据库迁移 | Prisma Migrate | 管理数据库结构变化 |
| 数据库 | PostgreSQL | 持久化业务数据 |
| 缓存 | Redis | 有明确需求再引入 |
| 后台任务 | BullMQ + Redis | 有队列、重试、独立 worker 需求再引入 |
| 测试 | Jest 或 Vitest，配合 Supertest | 单元与 HTTP 集成测试，沿用所选模板 |
| Lint / Format | 项目已有 Linter + Prettier | 可采用 ESLint；模板已有 oxlint 时先沿用 |
| 依赖管理 | npm 或 pnpm | 团队统一选择一个，并提交对应锁文件 |
| 配置 | `@nestjs/config` + 启动时校验 | 读取并验证配置 |
| 部署 | Docker + Docker Compose | 打包并运行服务 |

Prisma 是本培训的默认 ORM 示例；已有项目使用其他 ORM 时可继续沿用。NestJS 集成方式见 [Prisma 官方配方](https://docs.nestjs.com/recipes/prisma)，配置管理见 [NestJS 配置文档](https://docs.nestjs.com/techniques/configuration)，BullMQ 与 Redis 的配合见 [NestJS 队列文档](https://docs.nestjs.com/techniques/queues)。

#### 2.4.3 用一个模块理解 NestJS

| 概念 | 职责 | 工单示例 |
| --- | --- | --- |
| Module | 组织相关 Controller、Provider 及依赖 | `TicketsModule` |
| Controller | 接收 HTTP 请求、调用业务服务、返回结果 | `POST /api/tickets` |
| Service / Provider | 执行业务逻辑，按依赖注入方式复用 | 创建工单、检查状态变化 |
| DTO | 定义输入或输出的数据结构 | `CreateTicketDto` |
| Pipe | 转换或校验输入 | 检查标题必填和长度 |
| Guard | 判断请求是否允许进入处理逻辑 | 登录检查、角色限制 |
| Exception Filter | 统一异常到 HTTP 响应的转换 | 整理错误码和错误信息 |

依赖注入可以理解为：由框架创建和连接服务，在需要它的地方声明依赖。不要在每个 Controller 中随意创建数据库客户端或业务服务实例。

资源级权限通常还需要结合 Service 中的数据查询：登录成功只证明身份成立，不能证明可以修改任意一张工单。

#### 2.4.4 NestJS 后端目录示例

```text
backend/
├─ src/
│  ├─ main.ts                    # 启动、全局配置与路由前缀
│  ├─ app.module.ts              # 根模块
│  ├─ config/                    # 环境变量加载与校验
│  ├─ common/
│  │  ├─ guards/                 # 通用认证、权限守卫
│  │  ├─ filters/                # 异常处理
│  │  └─ decorators/             # 通用装饰器
│  ├─ database/
│  │  ├─ database.module.ts
│  │  └─ prisma.service.ts       # 数据库客户端生命周期
│  └─ modules/
│     ├─ auth/                  # 登录及身份集成
│     ├─ users/
│     └─ tickets/
│        ├─ dto/
│        │  ├─ create-ticket.dto.ts
│        │  └─ update-ticket.dto.ts
│        ├─ tickets.module.ts
│        ├─ tickets.controller.ts
│        ├─ tickets.service.ts
│        └─ tickets.service.spec.ts
├─ prisma/
│  ├─ schema.prisma              # 数据模型
│  ├─ migrations/                # 数据库迁移
│  └─ seed.ts                    # 可选：开发环境演示数据
├─ test/                         # HTTP 集成与端到端测试
├─ prisma.config.ts              # 按所用 Prisma 版本配置
├─ package.json
├─ package-lock.json             # 本示例选 npm
├─ tsconfig.json
├─ nest-cli.json
├─ .env.example
├─ Dockerfile
└─ README.md
```

选择 pnpm 时使用 `pnpm-lock.yaml`，不同时维护两套包管理器锁文件。Prisma 的配置位置、生成方式和驱动适配可能随大版本变化，应按项目锁定版本的文档初始化，避免混用不同年代的教程。

#### 2.4.5 参数校验必须真正启用

下面展示启动入口中的关键配置片段，具体放入项目现有启动函数：

```typescript
import { ValidationPipe } from '@nestjs/common';

app.useGlobalPipes(
  new ValidationPipe({
    whitelist: true,
    forbidNonWhitelisted: true,
    transform: true,
  }),
);
```

DTO 需要使用对应校验装饰器，例如 `@IsString()`、`@IsNotEmpty()`、`@MaxLength(100)`。只写 TypeScript `interface` 不会自动实现这些 HTTP 校验。转换数字、日期、布尔值时也要定义明确规则并验证实际行为。详见 [NestJS ValidationPipe](https://docs.nestjs.com/techniques/validation)。

#### 2.4.6 启动与检查方式

以下命令在已完成初始化、使用 npm 且存在锁文件的 `backend` 中运行：

```powershell
npm ci
npm run start:dev
```

在另一个终端运行项目实际配置的检查，例如：

```powershell
npm run build
npm run test
```

Lint、类型检查、端到端测试的脚本名称以 `package.json` 为准。团队可以统一为 `lint`、`typecheck`、`test:e2e`，但应先确保脚本真实存在，并在 README 写明执行目录。

### 2.5 前后端放在同一个仓库

本培训建议：**一个 Git 仓库，前端目录命名为 `frontend`，后端目录命名为 `backend`。** Python 和 NestJS 是后端二选一，不要求同一项目同时放两套。

#### 2.5.1 常见完整目录

```text
project/
├─ .git/                         # 整个项目共用一个 Git 仓库
├─ frontend/
│  ├─ src/
│  │  ├─ api/                    # 请求封装、生成客户端入口
│  │  ├─ components/             # 通用界面组件
│  │  ├─ features/
│  │  │  └─ tickets/             # 工单页面与业务交互
│  │  ├─ router/
│  │  └─ stores/                 # 有需要时使用
│  ├─ public/
│  ├─ package.json
│  ├─ package-lock.json          # 示例使用 npm
│  ├─ .env.example
│  └─ README.md
├─ backend/                      # 使用前面两种后端结构之一
├─ contracts/
│  └─ openapi.json               # 从后端导出的接口契约
├─ docs/
│  ├─ requirements.md
│  ├─ architecture.md
│  ├─ tasks.md
│  └─ handoff.md
├─ scripts/                      # 启动、生成契约等辅助脚本
├─ tests/
│  └─ e2e/                      # 跨前后端的业务流程测试
├─ .github/
│  └─ workflows/                 # 使用 GitHub 时的 CI 配置
├─ .gitignore
├─ .env.example                  # Compose 等根级配置示例
├─ compose.yaml
├─ AGENTS.md
└─ README.md
```

这是建议目录，已有前端工程可以保留自己的 `views`、`pages` 或路由约定，不必为了与图一致而重构。

#### 2.5.2 同仓库的好处与边界

- AI 可以在同一工作目录中追踪页面、接口和数据的关系。
- 一个功能的前后端修改、契约更新和测试可以放在同一个 PR 中审查。
- 新同事只需获取一个仓库，就能找到完整需求与运行说明。
- 接口变化时，便于同时更新调用方，减少“后端已经改了、前端尚未同步”的情况。

同仓库可以分别构建、分别部署。权限隔离要求强、团队发布节奏完全不同或服务独立复用较多时，再评估分仓库。

初期允许 `frontend` 与 `backend` 各自管理依赖。TypeScript 项目规模增长后，可按需使用 workspace；Python 后端也可以与 Node 前端自然共存，不要求统一成一种包管理器。

#### 2.5.3 把接口契约当作前后端协作边界

建议工作流：

```text
后端定义请求与响应
    → 生成 OpenAPI
    → 导出 contracts/openapi.json
    → 生成前端客户端和类型
    → 前端调用与联调
```

OpenAPI 文件在本方案中是生成产物，修改来源是后端接口定义；不要在多个地方分别手工维护接口规则。生成工具与版本需要固定，生成后在 CI 中检查是否存在未同步变化。

即使前后端都使用 TypeScript，也只共享适合公开的接口契约，避免让前端直接依赖数据库模型、内部服务代码或含敏感字段的对象。JSON 中的日期表示、可空字段和枚举也需要明确约定。

#### 2.5.4 本地联调的几个常见问题

| 问题 | 建议处理方式 |
| --- | --- |
| 前端和后端端口不同 | 在 README 固定端口；前端统一通过 `/api` 路径调用并配置开发代理 |
| 浏览器跨域错误 | 核实是否经过代理，或按实际来源配置 CORS；CORS 不代替权限 |
| Cookie 登录失败 | 核对请求是否携带凭据及 Cookie 的域、SameSite、Secure 设置 |
| 主机能连数据库，容器不能 | 区分宿主机端口和容器网络；Compose 内通常使用服务名连接 |
| 前端读取不到环境变量 | 核实构建工具的变量命名规则及是否需要重新启动或构建 |
| 密钥出现在前端构建产物 | 将 LLM、数据库等私密凭据移到后端，前端仅调用业务接口 |

数据库连接串、登录密钥和 LLM API Key 不进入 Git。提交 `.env.example` 说明需要哪些变量，真实值通过本地环境或部署系统提供。`.gitignore` 不能清除已经提交的密钥，误提交后需要撤销或轮换相关凭据。

### 2.6 两套技术栈都需要的工程约定

#### 2.6.1 先约定接口行为

| 项目 | 工单系统示例约定 |
| --- | --- |
| 路由 | `POST /api/tickets`、`GET /api/tickets` |
| ID | 对外统一为字符串，避免前端处理大整数时丢失精度 |
| 时间 | 使用带时区的 ISO 8601 字符串；存储与展示策略写清楚 |
| 列表 | 明确分页方式、默认条数、最大条数与排序 |
| 错误 | 统一业务错误码、可读提示和 requestId，保留合理 HTTP 状态码 |
| 登录身份 | 由后端认证结果提供，不信任请求体中的 userId |
| 权限 | 每次操作都检查资源归属或对应角色权限 |

错误响应可以采用如下结构；这只是本培训约定，不是框架强制要求：

```json
{
  "code": "TICKET_TITLE_REQUIRED",
  "message": "请填写工单标题",
  "requestId": "example-request-id"
}
```

具体使用 400 还是 422 等状态码可由项目约定，但前后端、文档和测试要一致。

#### 2.6.2 数据库迁移跟随代码交付

新增字段的流程：修改模型 → 生成迁移 → 审查迁移内容 → 在开发或测试库执行 → 测试旧数据和新功能 → 连同代码提交。

使用 Alembic 时，生成迁移前应配置正确的模型元数据；使用 Prisma 时，区分开发阶段创建迁移与部署阶段应用已有迁移。不能把删除数据库重建当作升级正式环境的常规办法。

例如给工单新增必填优先级时，需要决定历史记录的默认值；如果直接新增非空字段而不处理已有数据，迁移可能失败。让 AI 明确提出数据回填与兼容策略。

#### 2.6.3 测试与日志围绕真实故障

起步阶段优先验证：

- 成功创建并能重新查询。
- 非法输入被后端拒绝。
- 未登录和越权操作不能成功。
- 数据变化后，已有记录仍然可用。
- 服务失败时，页面能显示适当提示，日志能定位原因。

接口集成测试使用独立测试数据库，避免清理测试数据时影响开发或生产记录。数据库相关行为尽量用与实际环境一致的 PostgreSQL 验证。

日志记录时间、请求标识、路由、状态和关键错误，避免记录密码、完整令牌及不必要的个人信息。日志的价值是让下一次定位有证据。

#### 2.6.4 从本地运行到交付

Docker Compose 可用于本地数据库、后端和其他依赖的组织。数据库需要持久数据卷；镜像与依赖版本应固定到团队验证过的版本。

交付至少包含：启动说明、环境变量示例、数据库迁移、构建方式和关键验证步骤。上线前还需明确迁移由谁执行、日志在哪里、数据如何备份、失败后如何恢复。代码回退不一定能回退数据库变化。

### 2.7 把两部分结合：用 Codex 完成一个全栈功能

以下示例用于已经确认选用 NestJS 的项目；选择 Python 时替换后端技术栈，其他工程步骤保持一致。

```text
请读取 AGENTS.md、docs/requirements.md、docs/architecture.md。
前端位于 frontend，后端位于 backend，使用 TypeScript + NestJS。
数据库使用 PostgreSQL，ORM 使用项目已配置的 Prisma。

本轮实现“创建工单并在列表显示”：
- 标题必填，最多 100 字；描述可选，最多 2000 字。
- 创建人来自后端登录身份，状态默认为待处理。
- 沿用已有接口响应和错误格式。
- 保存到真实开发数据库，刷新页面后数据仍然存在。
- 表单包含提交中、成功和失败状态。

请先核对现有模块、数据库模型和接口契约，按确认的方案实现。
需要变更数据库时提供迁移，并说明对已有数据的影响。
更新 OpenAPI 与前端调用，运行相关检查。

交付时说明：
1. 页面、接口和数据分别改了什么。
2. 实际运行的检查及结果。
3. 未执行的检查与原因。
4. 我可以如何手工验收。
5. 更新后的任务状态和下一步。
```

建议现场操作顺序：先展示运行中的目标功能，再回到起始版本讨论需求并写入文档；随后让 AI 完成一条业务链路，演示正常提交、非法参数和越权请求，最后查看改动与交接文档。

课后练习可以是“增加优先级字段及筛选”。要求同事自己解释：改了哪些层、历史数据如何处理、哪些验证证明功能完成。练习的完成标准是能运行、能解释、能验证，而不是生成了多少文件。

### 2.8 培训后的日常工作速查

| 时机 | 建议动作 |
| --- | --- |
| 新需求开始 | 明确目标、范围、业务规则和验收标准 |
| 方案明确 | 写入 Markdown，检查假设与未决问题 |
| 开始实现 | 读取相关规则与方案，按小功能推进 |
| 发生错误 | 提供复现、预期、实际、日志和最近改动 |
| 准备交付 | 运行针对性检查，审查代码差异和数据变化 |
| 当天结束 | 更新交接，检查未提交修改，长会话按需压缩 |
| 下一天继续 | 读取交接并核对实际仓库状态 |

技术栈帮助统一实现方式；文档保存决定；Git 记录修改；测试提供证据。将这些环节连接起来，AI 才能持续参与项目开发。
